## Criando venv

```bash
python -m venv venv
```

## Primeiros passos

```bash
# Criando venv
python -m venv venv

# Ativando venv powershell
venv\Scripts\activate

# Instalando Django
pip install django

# Start project
django-admin startproject aula_django .
```